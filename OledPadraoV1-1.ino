//Rotinas padrao para o Oled Projetos Ober
// desliguei oled nao usando
/*
void OledInit() {
  pinMode(Vext,OUTPUT);
  digitalWrite(Vext, LOW);    // OLED USE Vext as power supply, must turn ON Vext before OLED init
  delay(50);
  // Initialising the UI will init the display too.
  display.init();
  display.flipScreenVertically(); //atualiza a tela verticalmente
  display.setTextAlignment(TEXT_ALIGN_LEFT); //alinhamento
  display.setFont(ArialMT_Plain_10); //tamanho da letra

  //display.setTextSize(1);      // Normal 1:1 pixel scale
  //display.setTextColor(SSD1306_WHITE); // Draw white text
  //display.setCursor(0, 0);     // Start at top-left corner
  //display.cp437(true);         // Use full 256 char 'Code Page 437' font
  display.clear();
}
 
 */
